2.1.5 кликаем по checkbox и radiobuttons (капча для роботов)

Автотест выполняет следующие шаги:
1. Открывает страницу http://suninjuly.github.io/math.html.
2. Находит значение для переменной Х .
3. Считывает математическую функцию от Х по формуле которая указана ниже.

import math                        #  используется модуль math

def calc(x):
  return str(math.log(abs(12*math.sin(int(x))))) # возвращает результат вычисления в строковом формате

4. Вводит ответ в текстовое поле.
5. Отмечает checkbox "i am the robot".
6. Выбирает radiobutton "Robots rule".
7. Кликает кнопку Submit.



